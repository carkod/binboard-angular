import { RoundDecimalsPipe } from './round-decimals.pipe';

describe('RoundDecimalsPipe', () => {
  it('create an instance', () => {
    const pipe = new RoundDecimalsPipe();
    expect(pipe).toBeTruthy();
  });
});
